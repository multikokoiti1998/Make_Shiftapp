<Window x:Class="Shiftapp_demo.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shiftapp_demo"
        mc:Ignorable="d"
        Title="管理者ウィンドウ"
        WindowState="Maximized"
        MinHeight="600" MinWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="技師情報管理" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" x:Name="TechniciansDataGrid"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="Auto"/>
                <DataGridTextColumn Header="氏名" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="土曜日勤務班" Binding="{Binding saturday_class}" Width="*"/>
                <DataGridTextColumn Header="カテーテル対応可否" Binding="{Binding catheterization}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
            <Button Content="新規技師追加フォーム表示" Padding="10,5" Margin="5" />
        
        </StackPanel>

        <TextBlock Grid.Row="3" Text="技師検索" FontSize="18" FontWeight="Bold" Margin="0,20,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="検索ID:" Margin="5" VerticalAlignment="Center"/>
            <TextBox x:Name="SearchKeywordTextBox" Width="200" Margin="5"/>
            <Button Content="検索" Width="80" Margin="5" Click="SearchTechnicians_Click"/>
            <Button Content="選択技師編集" Padding="10,5" Margin="5" Click="EditSelectedTechnician_Click"/>
            <Button Content="選択技師削除" Padding="10,5" Margin="5" Click="DeleteSelectedTechnician_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="5" x:Name="SearchResultsDataGrid"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  Margin="0,0,0,10"/>

        <TextBlock Grid.Row="6" Text="祝日設定" FontSize="20" FontWeight="Bold" Margin="0,20,0,10"/>


        <DataGrid Grid.Row="7" x:Name="HolidaysDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="日付" Binding="{Binding Date, StringFormat=yyyy-MM-dd}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="休日追加" Padding="10,5" Margin="5"/>
            <Button Content="選択休日削除" Padding="10,5" Margin="5"/>
        </StackPanel>

    </Grid>
</Window>