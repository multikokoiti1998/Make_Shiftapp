<Window x:Class="Shiftapp_demo.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Shiftapp_demo"
        mc:Ignorable="d"
        Title="AdminWindow" Height="1080" Width="1920">
    <Grid Margin="0,0,0,-6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="技師情報管理" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        <DataGrid Grid.Row="1" x:Name="TechniciansDataGrid"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"       Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="Auto"/>
                <DataGridTextColumn Header="氏名" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="連絡先" Binding="{Binding Contact}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
            <Button Content="新規技師追加フォーム表示" Width="180" Margin="5" Click="ShowAddTechnicianForm_Click"/>
            <Button Content="選択技師編集" Width="100" Margin="5" Click="EditSelectedTechnician_Click"/>
            <Button Content="選択技師削除" Width="100" Margin="5" Click="DeleteSelectedTechnician_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Text="新規技師追加" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="4" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="氏名:" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="0" Grid.Column="1" x:Name="NewTechnicianNameTextBox" Margin="5"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="カテーテル対応可能:" Margin="5" VerticalAlignment="Center"/>
                <TextBox Grid.Row="1" Grid.Column="1" x:Name="NewTechnicianContactTextBox" Margin="5"/>

            </Grid>
        </StackPanel>
        <Button Grid.Row="5" Content="この技師をデータベースに追加" Margin="5" Click="AddTechnicianToDatabase_Click"/>

        <TextBlock Grid.Row="6" Text="技師検索" FontSize="18" FontWeight="Bold" Margin="0,20,0,10"/>
        <StackPanel Grid.Row="7" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="検索キーワード:" Margin="5" VerticalAlignment="Center"/>
            <TextBox x:Name="SearchKeywordTextBox" Width="200" Margin="5"/>
            <Button Content="検索" Width="80" Margin="5" Click="SearchTechnicians_Click"/>
        </StackPanel>
        <DataGrid Grid.Row="8" x:Name="SearchResultsDataGrid"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  Margin="0,0,0,10"/>

        <TextBlock Grid.Row="9" Text="年間休日設定" FontSize="20" FontWeight="Bold" Margin="0,20,0,10"/>
        <DataGrid Grid.Row="10" x:Name="HolidaysDataGrid"
                  AutoGenerateColumns="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  Margin="0,0,0,10">
        </DataGrid>
        <StackPanel Grid.Row="11" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,0">
            <Button Content="休日追加" Width="100" Margin="5"/>
            <Button Content="選択休日削除" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
